
beforeSave

save

afterSave
	enqueue
	
	
	
job enqueue 补偿任务



异步处理：
	

	
每个任务需要一些对象，这个任务的创建是在请求来了之后，所以这些对象的引用如何获取，如何存在

在容器启动成功之后，通过固定的方法build一个全局的引用，每次需要使用的时候直接get就可以，这个上下文是个通用的
在每次创建任务都直接传递上下文和业务字段就可以，剩下交给任务做